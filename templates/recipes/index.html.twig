{% extends 'base.html.twig' %}

{% block body %}
    <ul>
        {% for recipe in recipes %}
            <li><a href="{{ path('app_recipe_view', {slug: recipe.slug}, false) }}">{{ recipe.title }}</a> - {{ recipe.difficulty }}</li>
        {% endfor %}
    </ul>
{% endblock %}
